# odo-webd configuration.
#
# Authentication is OFF by default (trusted-LAN model). To require a styled
# login before the dashboard is reachable, set a password below and restart
# odo-webd (/etc/init.d/S96odowebd restart). Leave it commented/empty for the
# open default.
#
#PASSWORD=changeme
